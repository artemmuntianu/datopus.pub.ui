export enum UserRole {
    HomeAdmin = 'HomeAdmin',
    HomeUser = 'HomeUser',
    PartnerUser = 'PartnerUser'
}

export enum OrgType {
    b2b = 'b2b',
    b2c = 'b2c'
}

export enum SubscriptionType {
    pro = 'pro',
    premium = 'premium'
}